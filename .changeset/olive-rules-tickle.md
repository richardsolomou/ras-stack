---
'ras-stack': patch
---

Advance the adoption baseline to 0.38.2 and point the shared workflows at that release. Repositories on an older line now report drift from `ras policy check adoption`, and `ras policy sync adoption` moves their package and workflow references forward.
