---
'ras-stack': minor
---

Tune shared defaults to how applications deploy: the Changesets policy targets private packages, the PostHog ingest proxy mounts at `/t` with segment-bound Vite routes, and half-configured provider credentials throw.
