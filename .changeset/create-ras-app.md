---
'create-ras-app': minor
'ras-stack': minor
---

Add the `create-ras-app` package so a production reference application can be generated with `pnpm create ras-app`. The wrapper calls the shared scaffold in-process, and both packages publish from the immutable release tag.
